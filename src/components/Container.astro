---
export interface Props {
    fullWidth?: boolean
    printHidden?: boolean
}

const { fullWidth, printHidden } = Astro.props;
---


<div class={(fullWidth ? "w-100 p-4" : "container mx-auto p-4") + (printHidden ? ' print:hidden' : '')}>
    <slot />
</div>